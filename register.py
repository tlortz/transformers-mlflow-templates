# mlflow start run
# parameterize the conda env and pyfunc model
# refer to them by relative path in Repos
# can I run the conda_env file to get the conda_env variables? Do I need to use dbutils.notebook.run()? 